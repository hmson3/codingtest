#include<bits/stdc++.h>
using namespace std;

#define x first
#define y second
string board[1002];
int fire[1002][1002];
int jihoon[1002][1002];
int n,m;
int dx[4] = {1,0,-1,0};
int dy[4] = {0,1,0,-1};

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    queue<int> q = {};

    while(true) {
        q.push(1);
        return 0;
    }
    cout << 1;

    return 0;
     
}
